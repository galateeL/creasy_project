if (window.location.href.indexOf("dashboard") > -1) {
    let a = document.getElementById("dashboard");
    a.className += " active text-white";
    a.setAttribute("aria-current", "page");
}

if (window.location.href.indexOf("companies") > -1) {
    let b = document.getElementById("companyNav");
    b.className += " active text-white";
    b.setAttribute("aria-current", "page");
}

if (window.location.href.indexOf("customer") > -1) {
    let c = document.getElementById("customerNav");
    c.className += " active text-white";
    c.setAttribute("aria-current", "page");
}

if (window.location.href.indexOf("map") > -1) {
    let c = document.getElementById("customerNav");
    c.className += " active text-white";
    c.setAttribute("aria-current", "page");
}

if (window.location.href.indexOf("prospect") > -1) {
    let d = document.getElementById("prospectNav");
    d.className += " active text-white";
    d.setAttribute("aria-current", "page");
}
if (window.location.href.indexOf("calendar") > -1) {
    let e = document.getElementById("calendarNav");
    e.className += " active text-white";
    e.setAttribute("aria-current", "page");
}










